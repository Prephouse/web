export enum SessionMedium {
  VideoAudio,
  AudioOnly,
}

export enum SessionType {
  Interview,
  Presentation,
}

export enum SessionOrigin {
  Record,
  Upload,
}

export enum InterviewType {
  Question,
  Session,
}
